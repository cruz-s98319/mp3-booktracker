import React from 'react';
import BookList from '../components/BookList';

const HomePage = () => {
    return (
        <div>
            <h1>Book Tracker</h1>
            <BookList />
        </div>
    );
};

export default HomePage;
